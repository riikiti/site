const swiper = new Swiper('.swiper', {
    // Optional parameters
    slidesPerView: 3,
    loop: true,
   // effect: "fade",
    grabCursor: true,
    centeredSlides: true,
    gap:0,
    autoplay: {
        delay: 5000,
    },

});